name: "Chat transcription submission"
description: "Submit a LaTeX transcription you produced in your own AI chat app (Tier 1)."
title: "[transcription] <work> — pages <n–m>"
labels: ["contribution", "transcription"]
body:
  - type: markdown
    attributes:
      value: |
        Thanks for contributing! Use the pinned prompt in `prompts/transcribe-chat.md`. It produces
        two things — the **LaTeX transcription** and the work's **`work.yaml`** — and there's a
        field for each below, so this submission is complete and ready to commit. You are credited.
  - type: input
    id: work
    attributes:
      label: Work (author, title, year)
      placeholder: "Bernhard Riemann, Über die Hypothesen…, 1868"
    validations: { required: true }
  - type: input
    id: scan
    attributes:
      label: Scan source (URL or stable identifier)
      placeholder: "https://…  /  digizeitschriften:252457811_0013"
    validations: { required: true }
  - type: input
    id: pages
    attributes:
      label: Pages covered
      placeholder: "133–140"
    validations: { required: true }
  - type: dropdown
    id: model
    attributes:
      label: AI model / app used
      options:
        - Claude (Anthropic)
        - ChatGPT (OpenAI)
        - Gemini (Google)
        - Other (note in the text box)
    validations: { required: true }
  - type: dropdown
    id: effort
    attributes:
      label: Thinking level (if you know it)
      options:
        - "unknown / standard"
        - "extended thinking on"
        - "reasoning model"
    validations: { required: false }
  - type: textarea
    id: workyaml
    attributes:
      label: work.yaml (metadata)
      description: "Paste the second code block the prompt produced. Do NOT include a copyright_assessment — the validator computes it."
      render: yaml
    validations: { required: true }
  - type: textarea
    id: latex
    attributes:
      label: LaTeX transcription
      description: "Paste the transcription (or attach a .tex file). Use the house-style macros."
      render: latex
    validations: { required: true }
  - type: input
    id: pubdate
    attributes:
      label: First-publication date + source
      description: "Skip if already in your work.yaml above. Otherwise cite a catalog/bibliography/DOI."
      placeholder: "1868 — Göttinger Abhandlungen vol. 13 (digizeitschriften:252457811_0013)"
    validations: { required: false }
  - type: input
    id: death
    attributes:
      label: Author death year(s) + source
      description: "Skip if already in your work.yaml above."
      placeholder: "1866 — wikidata:Q42299"
    validations: { required: false }
  - type: checkboxes
    id: license
    attributes:
      label: Licensing
      options:
        - label: "I release this contribution into the public domain under CC0 1.0."
          required: true
        - label: "This transcription is from the original/early edition, not a modern annotated edition."
          required: true
